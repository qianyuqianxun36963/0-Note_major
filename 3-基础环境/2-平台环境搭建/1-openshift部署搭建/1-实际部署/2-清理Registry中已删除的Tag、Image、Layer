<h1>清理Registry中已删除的Tag、Image、Layer</h1>

<p>在Master节点上操作。</p>

<p>查看可以清理的资源：</p>

<pre>
<code>oc adm prune images</code></pre>

<p>该命令会列出未被使用的blob，真正要删除的话需要在命令后加–comfirm参数：</p>

<pre>
<code>oc adm prune images --confirm</code></pre>

<p>参考资料：</p>

<p><a href="https://docs.openshift.org/latest/admin_guide/pruning_resources.html#pruning-images" rel="nofollow">https://docs.openshift.org/latest/admin_guide/pruning_resources.html#pruning-images</a></p>
