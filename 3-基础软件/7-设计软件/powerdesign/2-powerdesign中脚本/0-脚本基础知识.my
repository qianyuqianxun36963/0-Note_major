<h1>powerdesign 脚本</h1>

<p>使用的是vbs脚本</p>

<h2>内置变量取值</h2>

<p>powerdesign里面内置的全局变量，可以在powerdesign里的对话框中看到，例如表格。它对话框就有填：name、code、comment的地方。</p>

<p>然后，，我们在vbs脚本里就可以用tab.name、tab.code、tab.comment获取对应的值了。</p>

<h2>内置脚本</h2>

<p>powerdesign 内置了很多脚本，放在&nbsp; ***\powerdesigner16\VB Scripts 目录下面。</p>

<p>自己写的脚本，点击保存的时候，也可以保存在这里。可以将本目录下脚本复制到powerdesign安装目录里。</p>
