<h1>常用的插入操作</h1>

<h2>基本操作：</h2>

<p>alt + insert 可以在idea中调出插入小窗口，然后可以选择生成构造函数、getter和setter方法等。</p>

<p>你在哪里触发的插入操作，生成的代码就会从哪里插入。</p>

<h2>注意点：</h2>

<p>这些都特别简单，需特别提的是：</p>

<p>1、生成getter、setter方法的时候，可以选择builder模式。生成的就是builder模式的setter方法。</p>

<p>2、可以自动插入测试类，自动生成测试方法！</p>
